#include "main.h"

/**
 * _abs - function name
 * @n: number to check
 * Description: prints absolute value
 * Return: returns int
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
