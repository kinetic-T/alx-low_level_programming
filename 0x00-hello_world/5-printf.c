#include <stdio.h>

/**
 * main - Entry point
 * Description: program uses printf() to print strings
 * Return: always returns 0
 */

int main(void)
{

printf("with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
